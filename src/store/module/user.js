const state = {
  userName: 'NameNimabi'
}
const mutations = {
  //
}
const actions = {
  //
}

export default {
  // namespaced为true表示使用命名空间
  // 这样可以使用vuex内置的方法：createNamespacedHelpers
  // 在store.vue里面
  namespaced: true,
  state,
  mutations,
  actions
}
